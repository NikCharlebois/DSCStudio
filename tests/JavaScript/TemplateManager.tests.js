var assert = require("assert");
var TemplateManager = require("../../src/scripts/TemplateManager");
// commenting this out until I can figure out the compiler for ES6 tests to run

describe("FakeTest", function(){
  it("Should have some tests written soon!", function() {
    assert(true === true, "Dummy test");
  });
});
